# Bump version

I want to a have a scripts/bump.sh script that will bump a version, commit and tag,
example usage: `scripts/bump.sh patch`, it will take the last tag and bump it.

I also want to add a "gx version" command that will show the version of the
binary's release.

I want you to research the best practices for this in the Go-world and make a few suggestions.
