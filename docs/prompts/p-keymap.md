I want to free the "p" keymap so I can use "p" for pull and "P" for push.

- Change it so when I "yank" uncommited changes (using the "y" mapping) the app
  goes into a "yank" mode so the keymaps change.

  In this new mode you can only go up/down (arrows or j/k), press escape to
  abort and go back to "normal" mode or press "p" to paste (after which you go
  back to "normal" mode as well)
