# TMUX support

Add the following two mappings:

- "N" - similar to "n", after creating the new worktree, opens a new tmux
  session by the same name with the working directory set to the worktree's
  directory
- "T" - similar to "T", but opens a new tab instead of a new session
